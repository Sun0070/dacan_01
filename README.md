# DAKER Platform
### 긴급 인수인계 해커톤: 명세서만 보고 구현하라

데이터 경진대회(해커톤) 정보 탐색, 팀 구성, 제출, 랭킹 확인을 한 곳에서 할 수 있는 **풀스택 없는 React 웹 플랫폼**입니다.
백엔드 없이 **localStorage만으로** 모든 상태를 관리하며, 최초 실행 시 시드 데이터를 자동으로 생성합니다.

---

## 목차

1. [기술 스택](#기술-스택)
2. [실행 방법](#실행-방법)
3. [평가 항목 대응](#평가-항목-대응)
4. [페이지별 기능](#페이지별-기능)
   - [공통 — Navbar & 글로벌 검색](#공통--navbar--글로벌-검색)
   - [홈 (/)](#홈-)
   - [해커톤 목록 (/hackathons)](#해커톤-목록-hackathons)
   - [해커톤 상세 (/hackathons/:slug)](#해커톤-상세-hackathonsslug)
   - [팀 찾기 (/camp)](#팀-찾기-camp)
   - [랭킹 (/rankings)](#랭킹-rankings)
   - [나의 공간 (/me)](#나의-공간-me)
5. [확장 기능](#확장-기능)
6. [데이터 설계](#데이터-설계)
7. [프로젝트 구조](#프로젝트-구조)

---

## 기술 스택

| 항목 | 내용 |
|---|---|
| 프레임워크 | React 18 + TypeScript |
| 번들러 | Vite 5 |
| 라우터 | React Router v6 |
| 스타일링 | Tailwind CSS v3 (`darkMode: 'class'`) |
| 데이터 저장 | `localStorage` (백엔드 없음) |
| 폰트 | Pretendard Variable (CDN) |
| 아이콘 | 커스텀 SVG 아이콘 시스템 (30개+) |

---

## 실행 방법

```bash
# 1. 의존성 설치
npm install

# 2. 개발 서버 실행
npm run dev
# → http://localhost:5173 에서 확인

# 3. 프로덕션 빌드
npm run build

# 4. 빌드 결과 로컬 미리보기
npm run preview
```

> 별도 설정 없이 실행하면 앱 최초 실행 시 8개 해커톤, 18개 팀, 6개 리더보드 데이터가 자동으로 생성됩니다.

---

## 평가 항목 대응

| 항목 | 배점 | 구현 내용 |
|---|:---:|---|
| **기본 구현** | 30 | 6개 페이지 완성 (Home / Hackathons / HackathonDetail / Camp / Rankings / MyDashboard), 북마크·팀·제출 localStorage CRUD |
| **확장 아이디어** | 30 | 다크 모드, 글로벌 검색(Ctrl+K), 스켈레톤 로딩, 토스트 알림, 해커톤 타임라인, 스킬 기반 팀 추천, 제출 이력, 사이드바 필터 통합 레이아웃 |
| **완성도** | 25 | Pretendard 폰트, SVG 아이콘 시스템, 3개 목록 페이지 통일된 사이드바 레이아웃, 반응형 디자인, 호버·애니메이션, 다크 모드 전체 대응 |
| **문서** | 15 | 본 README |

---

## 페이지별 기능

---

### 공통 — Navbar & 글로벌 검색

모든 페이지 상단에 고정되는 네비게이션 바입니다.

**Navbar**

| 요소 | 동작 |
|---|---|
| `[DA]` 로고 | 클릭 시 홈(`/`)으로 이동 |
| 네비게이션 링크 | 해커톤 / 팀 찾기 / 랭킹 / 나의 공간, 현재 페이지 인디고 하단 보더 표시 |
| 검색 버튼 | 클릭 또는 `Ctrl+K` (`Cmd+K`) 단축키로 검색 모달 열기 |
| 다크 모드 버튼 | Sun/Moon 아이콘 토글, 설정 localStorage 유지 |
| 모바일 햄버거 | 768px 이하에서 드롭다운 메뉴 표시 |

**글로벌 검색 모달 (`Ctrl+K`)**

- `Ctrl+K` / `Cmd+K` 또는 검색 버튼으로 열기, `Esc` / 배경 클릭으로 닫기
- 입력과 동시에 실시간 결과 반영
- **검색 대상**: 해커톤 제목·태그, 팀 이름·소개
- **결과 그룹화**: 해커톤 / 팀 섹션으로 분리 표시, 항목 클릭 시 해당 페이지로 이동

---

### 홈 (`/`)

서비스 진입점 페이지입니다.

**히어로 섹션**
- 그리드 패턴 배경 + 방사형 그라디언트 오버레이
- 인디고-바이올렛 그라디언트 메인 헤드라인
- `해커톤 탐색` → `/hackathons`, `팀 찾기` → `/camp` 버튼
- 진행 중인 해커톤이 있을 때 실시간 펄스 뱃지 표시

**기능 카드 (3개)**
- 해커톤 / 팀 찾기 / 랭킹 카드에 실시간 수치 표시
  - 대회 수 / 모집 중인 팀 수 / 총 제출 수
- 호버 시 카드 부상 + 그림자 효과

**통계 행**
- 전체 해커톤 수 / 모집 중인 팀 수 / 총 제출 수를 3분할 표시

**이용 방법 (3단계)**
- ① 해커톤 탐색 → ② 팀 구성 → ③ 제출 & 랭킹
- 각 단계 사이 커넥터 라인으로 흐름 시각화

**주요 해커톤 섹션**
- 진행중 → 예정 → 종료 순으로 최대 3개 카드 표시

---

### 해커톤 목록 (`/hackathons`)

**레이아웃**: 좌측 사이드바 + 우측 카드 그리드

#### 좌측 사이드바 (데스크탑 고정, 스티키)

| 필터 | 동작 |
|---|---|
| 검색 입력 | 제목·태그 실시간 필터링, X 버튼으로 초기화 |
| 상태 필터 | 전체 / 진행중 / 예정 / 종료 — 각 상태 카운트 뱃지, 진행중 항목 펄스 |
| 정렬 | 최신순(종료일 기준) / 마감임박순(제출 마감일 기준) |
| 태그 필터 | 전체 태그를 칩으로 표시, 클릭 시 필터 적용 |
| 초기화 버튼 | 활성 필터가 있을 때만 노출 |

#### 우측 카드 패널

- 결과 수 표시 (필터 적용 시 "N개 결과" / 전체 시 "N개의 해커톤")
- 반응형 그리드: 모바일 1열 → 태블릿 2열 → 데스크탑 3열
- 결과 없을 시 빈 상태 안내 + 필터 초기화 버튼

#### HackathonCard 컴포넌트

- 상태별 3px 상단 컬러 보더: 진행중 `emerald` / 예정 `violet` / 종료 `slate`
- D-day 뱃지: D-3 이하 `rose` / D-14 이하 `amber` / 그 외 `slate`
- 우측 상단 북마크 버튼 (localStorage 저장)
- 호버 시 `-translate-y-1` 부상 + 그림자 효과

---

### 해커톤 상세 (`/hackathons/:slug`)

**상단 헤드 영역**
- 상태 뱃지 + D-day 뱃지
- 해커톤 제목, 태그 목록
- 제출 마감일 / 종료일
- 북마크 버튼 / 외부 링크 버튼

**8탭 스티키 탭 바** — 스크롤 시 상단 고정

| 탭 | 주요 내용 |
|:---:|---|
| 개요 | 대회 소개 요약, 팀 구성 정책 (솔로 허용 여부, 최대 인원) |
| 안내 | 공지사항 목록, 규칙/FAQ 외부 링크 버튼 |
| 평가 | 평가 메트릭 설명, 일일 최대 제출 횟수, 런타임 제한, 점수 구성 비율 |
| 일정 | 마일스톤 타임라인 — 과거(체크)/현재(펄스)/미래(빈원) 시각적 구분 |
| 상금 | 1~3위 메달 아이콘 + 상금 금액 카드 |
| 팀 | 해당 해커톤 등록 팀 목록, 팀 찾기 페이지 이동 링크 |
| 제출 | 허용 아티팩트 유형, 제출 가이드, 제출 폼 |
| 리더보드 | 순위표 (팀명 / 점수 / 제출 시각), 점수 구성 세부 표시 |

**제출 폼**
- 팀 이름, 플랜 제목, 웹 URL, PDF URL 입력
- 제출 시 localStorage 저장 + 성공 토스트 알림

---

### 팀 찾기 (`/camp`)

**레이아웃**: 좌측 사이드바 + 우측 팀 카드 그리드

#### 좌측 사이드바 (데스크탑 고정, 스티키)

| 필터 | 동작 |
|---|---|
| 검색 입력 | 팀 이름·소개 실시간 필터링 |
| 모집 상태 필터 | 전체 / 모집중 / 마감 — 카운트 뱃지 + 색상 도트 |
| 해커톤 필터 | 진행중/예정/종료 그룹별 목록, 각 해커톤의 등록 팀 수 뱃지 |
| 역할 필터 | Frontend / Backend / Designer / ML Engineer / Data Scientist / PM |
| 초기화 버튼 | 활성 필터가 있을 때만 노출 |

#### 우측 팀 카드 패널

- **내 스킬 기반 추천 팀** (나의 공간에서 스킬 설정 시 상단 노출)
  - 내 스킬과 매칭되는 모집중 팀을 인디고 배경 섹션으로 강조 표시
- 결과 수 표시
- 반응형 그리드: 모바일 1열 → 태블릿 2열 → 데스크탑 3열

#### 팀 만들기 / 수정 모달

- **기본 정보**: 팀 이름(필수), 해커톤 선택(필수), 팀 소개
- **모집 설정**: 모집 여부 토글 스위치, 현재 인원 스텝퍼(1~10), 찾는 역할 다중 선택
- **연락처**: 오픈채팅 등 URL 입력
- 모달 외부 클릭 / 취소 버튼으로 닫기

#### TeamCard 컴포넌트

- 팀 코드 해시 기반 결정적 색상 아바타 (6가지 색상)
- 모집 상태 뱃지: 모집중 `emerald` / 마감 `slate`
- 멤버 수 도트 인디케이터 (최대 5dot 표시)
- 내가 만든 팀에만 수정/삭제 버튼 노출
- 삭제 시 확인 다이얼로그 표시

---

### 랭킹 (`/rankings`)

**레이아웃**: 좌측 사이드바 + 우측 리더보드 패널

#### 좌측 사이드바 (데스크탑 고정, 스티키) — 3단 구성

| 섹션 | 내용 |
|---|---|
| 검색창 | 해커톤 이름 실시간 필터링, 항상 최상단 고정 |
| 내 참여 대회 | 내가 팀을 만든 해커톤 목록, 현재 순위 뱃지 표시, 검색 시 자동 숨김 |
| 대회 목록 | 진행중 / 예정 / 종료 그룹별 전체 해커톤 목록, 각 항목에 참여 팀 수 뱃지 |

- 각 섹션은 Camp·Hackathons 사이드바와 동일한 디자인 패턴 적용 (섹션 헤더 uppercase, 항목 라운드 버튼, 활성 항목 인디고 배경)
- 앱 로드 시 첫 번째 진행중 해커톤 자동 선택
- 사이드바 내부 독립 스크롤 지원

#### 우측 리더보드 패널

- 선택된 해커톤 제목 + 마지막 업데이트 시각
- **TOP 3 포디움**: 1~3위 팀을 메달 아이콘(금/은/동)과 높이 차이로 시각화
- **순위표**: 팀명, 점수, 제출 시각, 참가자·심사위원 세부 점수
- **내 팀 강조**: 내가 만든 팀의 행은 인디고 배경 + "내 팀" 뱃지로 구분
- 아티팩트 버튼: 웹 사이트 링크 / PDF 보고서 링크
- 리더보드가 없는 해커톤 선택 시 빈 상태 안내

**모바일 대응**: 상단 드롭다운 셀렉터로 해커톤 선택

---

### 나의 공간 (`/me`)

#### 프로필 섹션

- 그라디언트 스쿼클 아바타 (이름 이니셜 자동 표시)
- 이름, 역할, 스킬 칩 목록 표시
- 프로필 편집 버튼

#### 프로필 편집 모달

- 이름 입력
- 역할 선택: Frontend / Backend / Designer / ML Engineer / Data Scientist / PM
- 스킬 다중 선택: Python / JavaScript / TypeScript / React / Node.js / Machine Learning / Deep Learning / SQL / Docker / Figma
- 커스텀 스킬 직접 입력 추가
- 저장 즉시 팀 찾기 추천 기능에 반영

#### 빠른 이동 네비게이션

북마크한 해커톤 / 내 팀 / 제출 이력 섹션으로 스크롤 이동 버튼

#### 북마크한 해커톤 섹션

- 북마크한 해커톤을 카드 그리드로 표시
- 카드에서 직접 북마크 해제 가능
- 북마크 없을 시 해커톤 탐색 링크 안내

#### 내 팀 섹션

- 내가 만든 팀을 카드로 표시
- 팀 수정 / 삭제 기능 (삭제 시 확인 다이얼로그)
- 팀 없을 시 팀 만들기 링크 안내

#### 제출 이력 섹션

- 내가 제출한 모든 내역을 타임라인으로 표시
- 해커톤명 / 팀명 / 제출 시각
- 플랜 제목 / 웹 URL / PDF URL 아티팩트 링크 버튼

---

## 확장 기능

### 다크 모드

- Navbar의 Sun/Moon 아이콘 버튼으로 즉시 전환
- Tailwind CSS `darkMode: 'class'` 방식, `document.documentElement` 에 `dark` 클래스 토글
- `daker_theme` 키로 localStorage에 저장, 새로고침 후에도 유지
- 모든 페이지 / 컴포넌트 / 모달에 다크 스타일 완전 대응

### 스켈레톤 로딩

- 데이터 로드 전 shimmer 애니메이션 스켈레톤 표시
- 해커톤 카드형 / 팀 카드형 두 가지 variant 지원

### 토스트 알림

- 화면 우하단에 스택 형태로 쌓임
- 3초 후 자동 소멸
- 타입별 색상 구분: `success` (에메랄드) / `error` (로즈) / `info` (슬레이트)
- 사용 시점: 팀 생성/수정/삭제, 제출 완료, 프로필 저장

### 해커톤 타임라인

- 해커톤 상세 → 일정 탭에서 마일스톤 시각화
- 상태 구분: 과거(회색 체크) / 현재(인디고 펄스 애니메이션) / 미래(빈 원)

### 스킬 기반 팀 추천

- 나의 공간에서 스킬 설정 시 팀 찾기 페이지 상단에 추천 섹션 자동 노출
- 내 스킬과 `lookingFor`가 겹치는 모집중 팀을 인디고 배경으로 강조

### 북마크 시스템

- 해커톤 카드 북마크 아이콘으로 저장/해제
- `UserProfile.bookmarks` 배열에 slug 저장
- 홈 / 해커톤 목록 / 해커톤 상세 / 나의 공간 전 페이지에서 상태 동기화

### 랭킹 내 팀 인식

- `UserProfile.myTeamCodes` 기반으로 내가 만든 팀을 리더보드에서 자동 식별
- 랭킹 사이드바 **"내 참여 대회"** 섹션: 내 팀이 있는 해커톤만 최상단에 모아 표시, 현재 순위 뱃지 함께 노출
- 리더보드 테이블에서 내 팀 행은 인디고 배경 하이라이트 + **"내 팀"** 뱃지로 시각적 구분

### 통일된 사이드바 레이아웃

해커톤 목록 / 팀 찾기 / 랭킹 세 페이지가 동일한 사이드바 디자인 패턴을 공유합니다.

- 섹션 헤더: uppercase 추적 텍스트
- 항목: 라운드 버튼, 우측 카운트 뱃지
- 활성 항목: 인디고 배경 + 인디고 텍스트 + 인디고 뱃지
- 데스크탑 스티키 + 내부 독립 스크롤, 모바일 토글 패널

### 반응형 레이아웃

| 화면 크기 | 레이아웃 |
|---|---|
| 모바일 (< 768px) | 햄버거 메뉴, 필터 토글 버튼으로 사이드바 열기 |
| 태블릿 (768px~1024px) | 2컬럼 카드 그리드 |
| 데스크탑 (> 1024px) | 스티키 사이드바 + 카드 그리드 전체 레이아웃 |

---

## 데이터 설계

### localStorage 키 목록

| 키 | 타입 | 설명 |
|---|---|---|
| `daker_seeded` | `"1"` | 초기 시드 완료 여부 (1회만 실행) |
| `daker_hackathons` | `Hackathon[]` | 해커톤 목록 |
| `daker_hackathon_details` | `HackathonDetail[]` | 해커톤 상세 데이터 |
| `daker_teams` | `Team[]` | 전체 팀 목록 |
| `daker_leaderboards` | `LeaderboardData[]` | 리더보드 데이터 |
| `daker_submissions` | `Submission[]` | 제출 이력 |
| `daker_profile` | `UserProfile` | 내 프로필 (이름 / 역할 / 스킬 / 북마크 / 팀코드) |
| `daker_theme` | `"light" \| "dark"` | 테마 설정 |

### 초기 시드 데이터 (자동 생성)

| 데이터 | 수량 | 상세 |
|---|:---:|---|
| 해커톤 | 8개 | 종료 3 / 진행중 2 / 예정 3 |
| 팀 | 18개 | 다양한 해커톤에 분산 배치 |
| 리더보드 | 6개 | 해커톤별 순위 데이터 |
| 제출 이력 | 3개 | FlowMasters / WaveRiders / TextMinds 팀 제출 내역 |
| 유저 프로필 | 1개 | 김다커 / ML Engineer / Python·PyTorch·ML 등 5개 스킬, 해커톤 3개 북마크, 팀 2개 소유 |

> 시드 데이터는 모든 기능(나의 공간 북마크·내 팀·제출 이력, 랭킹 내 팀 강조, 팀 찾기 스킬 추천)을 즉시 확인할 수 있도록 구성되어 있습니다.

### 등록된 해커톤 목록

| 상태 | 제목 | 주요 태그 |
|:---:|---|---|
| 종료 | 컴퓨터 비전 챌린지 : 객체 탐지 정확도 최적화 | CV, ObjectDetection, YOLO |
| 종료 | NLP 감성 분석 해커톤 : 리뷰 데이터로 감정 읽기 | NLP, BERT, HuggingFace |
| 종료 | Aimers 8기 : 모델 경량화 온라인 해커톤 | LLM, Compression, vLLM |
| 진행중 | 월간 해커톤 : 바이브 코딩 개선 AI 아이디어 공모전 | GenAI, Workflow, Idea |
| 진행중 | 시계열 예측 챌린지 : 전력 수요 패턴 예측 대회 | TimeSeries, LSTM, XGBoost |
| 예정 | 긴급 인수인계 해커톤: 명세서만 보고 구현하라 | VibeCoding, Web, Vercel |
| 예정 | 추천 시스템 챌린지 : 클릭률 예측 & 개인화 랭킹 | RecSys, CTR, Collaborative |
| 예정 | 멀티모달 AI 해커톤 : 이미지 + 텍스트 융합 챌린지 | Multimodal, VLM, CLIP |

---

## 프로젝트 구조

```
daker-platform/
├── src/
│   ├── components/
│   │   ├── Navbar.tsx          # 상단 네비게이션 (검색, 다크모드, 모바일 메뉴)
│   │   ├── Layout.tsx          # 공통 레이아웃 (푸터 포함)
│   │   ├── HackathonCard.tsx   # 해커톤 카드 (상태 보더, D-day, 북마크)
│   │   ├── TeamCard.tsx        # 팀 카드 (아바타, 역할 칩, 멤버 도트)
│   │   ├── SkeletonCard.tsx    # shimmer 로딩 스켈레톤
│   │   ├── Timeline.tsx        # 마일스톤 타임라인
│   │   ├── SearchModal.tsx     # Ctrl+K 글로벌 검색 모달
│   │   └── Icons.tsx           # SVG 아이콘 30개+ 모음
│   ├── context/
│   │   ├── ThemeContext.tsx    # 다크/라이트 모드 전역 상태
│   │   └── ToastContext.tsx    # 토스트 알림 전역 상태
│   ├── data/
│   │   ├── hackathons.ts       # 해커톤 초기 데이터 (8개)
│   │   ├── hackathonDetails.ts # 해커톤 상세 초기 데이터 (6개)
│   │   ├── teams.ts            # 팀 초기 데이터 (18개)
│   │   └── leaderboard.ts      # 리더보드 초기 데이터 (6개)
│   ├── lib/
│   │   ├── storage.ts          # localStorage 타입 안전 래퍼 함수
│   │   └── seed.ts             # 앱 최초 실행 시 시드 데이터 주입
│   ├── pages/
│   │   ├── Home.tsx            # 홈 (/)
│   │   ├── Hackathons.tsx      # 해커톤 목록 (/hackathons)
│   │   ├── HackathonDetail.tsx # 해커톤 상세 (/hackathons/:slug)
│   │   ├── Camp.tsx            # 팀 찾기 (/camp)
│   │   ├── Rankings.tsx        # 랭킹 (/rankings)
│   │   ├── MyDashboard.tsx     # 나의 공간 (/me)
│   │   └── NotFound.tsx        # 404 페이지
│   └── types/index.ts          # TypeScript 인터페이스 정의
├── tailwind.config.js
├── vite.config.ts
└── package.json
```

## 1. 프로젝트 제목

# DAKER — 데이터 경진대회 팀 매칭 플랫폼

---

## 2. 서비스 개요

### 서비스명
**DAKER** (Data + Hacker의 합성어)

### 한줄 소개
데이터 경진대회 정보 탐색부터 팀 구성, 제출, 순위 확인까지 — 해커톤 참가자를 위한 올인원 플랫폼

### 개발 목표
데이터 해커톤 참가자들이 여러 사이트를 오가며 대회 정보를 수집하고, 카카오톡 오픈채팅이나 커뮤니티 게시글로 팀원을 구하는 비효율적인 과정을 단일 플랫폼으로 통합한다. 나아가 사용자 프로필(역할·스킬)을 기반으로 적합한 대회와 팀을 자동 추천하여, 참가 결정부터 팀 합류까지의 마찰을 최소화한다.

### 타겟 사용자

| 유형 | 설명 |
|------|------|
| **입문 참가자** | 처음 해커톤에 참가하려는 ML/데이터 입문자. 어떤 대회가 있는지, 어떤 팀에 합류해야 하는지 막막한 상황 |
| **중급 참가자** | 여러 대회 경험이 있고, 자신의 스킬에 맞는 대회를 빠르게 찾아 좋은 팀원과 조합하고 싶은 사용자 |
| **팀 리더** | 팀을 구성하고 역할별 팀원을 모집하려는 사용자. 모집 공고 작성·관리가 필요 |
| **관람자/응원자** | 리더보드를 통해 결과를 확인하고 싶은 사용자 |

### 기대효과

- **시간 절약**: 대회 탐색 → 팀 합류까지 하나의 서비스에서 완결
- **미스매칭 감소**: 역할·스킬 기반 AI 추천으로 잘 맞는 팀과 대회를 먼저 노출
- **참여율 향상**: 팀 모집 공고와 연락처가 한 곳에 모여 팀 합류 장벽 감소
- **성과 추적**: 리더보드·제출 이력·최고 순위 통계로 자신의 성장 기록 보관

---

## 3. 페이지 구성

### 페이지 간 연결 관계

```
홈 (/)
 ├── 해커톤 목록 (/hackathons)
 │    └── 해커톤 상세 (/hackathons/:slug)
 │         └── 팀 찾기 (/camp) ← 팀 탭 연동
 ├── 팀 찾기 (/camp)
 ├── 랭킹 (/rankings)
 └── 나의 공간 (/me)
      ├── 참여중인 해커톤 → 해커톤 상세
      ├── 북마크 해커톤 → 해커톤 상세
      └── 내 팀 → 팀 찾기
```

### 페이지별 설명

| 페이지 | 주요 표시 내용 | 가능한 행동 |
|--------|--------------|-------------|
| **홈** `/` | 플랫폼 소개, 바로가기 카드, 통계, 주요 해커톤 3종 | 해커톤·팀 찾기·랭킹 페이지로 이동 |
| **해커톤** `/hackathons` | 전체 해커톤 목록 (상태·태그 필터, 정렬), 맞춤 추천 캐러셀 | 필터·검색·북마크, 추천 캐러셀 페이지 이동, 카드 클릭 → 상세 이동 |
| **해커톤 상세** `/hackathons/:slug` | 개요·평가·일정·시상·팀·제출·리더보드 탭 | 북마크, 링크 공유, 결과물 제출, 탭 전환 |
| **팀 찾기** `/camp` | 전체 팀 목록 (상태·해커톤·역할 필터), 맞춤 팀 추천 캐러셀 | 팀 만들기/수정/삭제, 팀 연락하기, 추천 캐러셀 탐색 |
| **랭킹** `/rankings` | 해커톤별 순위표, 점수 breakdown, 제출물 링크 | 전체 리더보드 / 나의 랭킹 탭 전환, 해커톤 선택 |
| **나의 공간** `/me` | 프로필, 통계 4종, 참여중인 해커톤, 북마크, 내 팀, 제출 이력 | 프로필 편집, 북마크 토글, 팀 수정/삭제, 섹션 빠른 이동 |

---

## 4. 시스템 구성

```
┌─────────────────────────────────────────────────────────┐
│                     브라우저 (SPA)                       │
│                                                         │
│  React 18 + TypeScript + Vite                          │
│  React Router v6 (클라이언트 사이드 라우팅)              │
│  Tailwind CSS v3 (스타일링)                             │
│                                                         │
│  ┌──────────────┐   ┌──────────────┐   ┌────────────┐  │
│  │   Pages      │   │  Components  │   │    Lib     │  │
│  │ Home         │   │ HackathonCard│   │ storage.ts │  │
│  │ Hackathons   │   │ TeamCard     │   │ seed.ts    │  │
│  │ HackathonDetail│  │ Navbar       │   │ recommend.ts│ │
│  │ Camp         │   │ SearchModal  │   └────────────┘  │
│  │ Rankings     │   │ SkeletonCard │                   │
│  │ MyDashboard  │   └──────────────┘                   │
│  └──────────────┘                                       │
│                                                         │
│  ┌──────────────────────────────────────────────────┐   │
│  │              localStorage (데이터 영속성)          │   │
│  │  hackathons · hackathon_details · teams           │   │
│  │  leaderboards · submissions · profile · theme     │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                          │ 빌드 결과물 배포
                          ▼
                    Vercel (정적 호스팅)
```

**외부 의존성 없음** — 백엔드 서버, 데이터베이스, 외부 API 없이 브라우저 localStorage만으로 모든 데이터를 관리합니다. 첫 방문 시 시드 데이터를 자동 초기화(`daker_seeded_v4`)하여 즉시 체험 가능한 상태를 제공합니다.

---

## 5. 핵심 기능 명세

### 5-1. 해커톤 탐색 및 필터링

| 기능 | 입력 | 동작 | 결과 |
|------|------|------|------|
| 상태 필터 | 사이드바 '진행중 / 예정 / 종료' 클릭 | 선택한 상태의 해커톤만 필터링 | 해당 상태 카드만 그리드에 표시 |
| 태그 필터 | 사이드바 태그 버튼 클릭 | 선택한 태그를 포함한 해커톤 필터링 | 해당 태그 카드만 그리드에 표시 |
| 마감임박순 정렬 | '마감임박순' 라디오 선택 | 종료 대회는 맨 뒤, 활성 대회는 마감일 오름차순 정렬 | 가장 빨리 마감되는 대회가 상단에 표시 |
| 키워드 검색 | 검색창에 텍스트 입력 | 제목·태그에 검색어 포함 여부 실시간 필터 | 일치하는 해커톤 카드만 표시, 없으면 빈 상태 안내 |
| 북마크 | 카드 우측 상단 북마크 아이콘 클릭 | 프로필의 bookmarks 배열에 slug 추가/제거 | 아이콘 색상 변경(황색 ↔ 회색), 나의 공간에 반영 |

### 5-2. AI 맞춤 추천 캐러셀

| 기능 | 입력 | 동작 | 결과 |
|------|------|------|------|
| 해커톤 추천 | 나의 공간에서 역할·스킬 설정 | 역할 태그 매칭(+15), 스킬 태그 매칭(+10/개), 상태 보너스(+10/+5) 합산 후 60점 만점 환산 | 70% 이상 대회만 점수 순 캐러셀 표시 (3개/페이지) |
| 팀 추천 | 나의 공간에서 역할·스킬 설정 | 역할 일치(+40), 스킬 일치(+10/개), 소개글 키워드(+5/개), 참여 이력(+20), 모집중(+5) 합산 후 60점 만점 환산 | 70% 이상 팀만 점수 순 캐러셀 표시 (3개/페이지) |
| 캐러셀 페이지 이동 | 카드 영역 외부 좌/우 화살표 버튼 클릭 | 현재 페이지 인덱스 ±1 변경 | 다음/이전 3개 카드 표시, 첫/마지막 페이지에서 해당 버튼 비활성 |

### 5-3. 팀 관리

| 기능 | 입력 | 동작 | 결과 |
|------|------|------|------|
| 팀 만들기 | '팀 만들기' 버튼 → 모달 폼 작성 → 저장 | 팀 객체 생성, localStorage에 저장, 프로필 myTeamCodes에 팀코드 추가 | 팀 목록에 즉시 반영, 나의 공간 팀 섹션에 표시, Toast 알림 |
| 팀 수정 | 내 팀 카드의 수정 버튼 → 모달 수정 → 저장 | 해당 teamCode의 팀 정보 업데이트 | 카드 내용 즉시 갱신, Toast 알림 |
| 팀 삭제 | 내 팀 카드의 삭제 버튼 → 확인 모달 → 삭제 | 팀 삭제, 프로필 myTeamCodes에서 제거 | 목록에서 즉시 제거, Toast 알림 |
| 연락하기 | 팀 카드 하단 '연락하기' 버튼 클릭 | 팀에 등록된 외부 URL(카카오오픈채팅/구글폼 등) 새 탭으로 열기 | 외부 연락 채널로 이동 |

### 5-4. 결과 제출

| 기능 | 입력 | 동작 | 결과 |
|------|------|------|------|
| 제출 | 해커톤 상세 '제출' 탭 → 폼 입력 → 제출 버튼 | 제출 객체 생성, localStorage submissions에 저장 | 제출 이력에 기록, 나의 공간 제출 이력 섹션에 표시 |

### 5-5. 랭킹 조회

| 기능 | 입력 | 동작 | 결과 |
|------|------|------|------|
| 전체 리더보드 | 해커톤 드롭다운 선택 | 선택한 해커톤의 전체 순위 데이터 로드 | 순위·팀명·점수·제출시각 테이블, 1~3위 메달 아이콘 표시 |
| 나의 랭킹 | '나의 랭킹' 탭 클릭 | 전체 리더보드 중 프로필 myTeamCodes 소속 팀 항목만 필터 | 내 팀 순위만 강조 표시 |

### 5-6. 나의 공간

| 기능 | 입력 | 동작 | 결과 |
|------|------|------|------|
| 프로필 편집 | '편집' 버튼 → 이름·역할·스킬 수정 → 저장 | 프로필 업데이트, 추천 점수 재산출 기반 변경 | 이후 해커톤/팀 추천 결과가 새 프로필 기준으로 변경 |
| 참여중인 해커톤 확인 | 페이지 진입 | 내 팀의 hackathonSlug 기준으로 해커톤 카드 렌더링 | 내가 팀으로 참여한 모든 해커톤 카드 표시 |
| 최고 순위 확인 | 페이지 진입 | 전체 리더보드에서 내 팀명 매칭 후 최소 rank 값 추출 | 통계 카드에 '몇 위' 표시 |

---

## 6. 주요 사용 흐름

### 정상 흐름 — 첫 방문부터 팀 합류까지

```
1. 첫 방문
   └── localStorage에 daker_seeded_v4 플래그 없음 → 시드 데이터 자동 초기화
       (해커톤 11개, 팀 26개, 리더보드, 제출 이력 등 샘플 데이터 삽입)

2. 나의 공간 (/me) → 프로필 설정
   └── 이름 입력, 역할(ML Engineer 등) 선택, 스킬(Python, PyTorch 등) 선택 → 저장

3. 해커톤 탐색 (/hackathons)
   └── 상단 맞춤 추천 캐러셀에서 70% 이상 매칭 대회 확인
   └── 마음에 드는 대회 북마크 또는 카드 클릭 → 상세 페이지 이동

4. 해커톤 상세 (/hackathons/:slug)
   └── 개요·평가·일정·시상 확인
   └── '팀' 탭 → 팀 찾기 페이지로 이동

5. 팀 찾기 (/camp)
   └── 맞춤 팀 추천 캐러셀에서 역할·스킬 매칭 팀 확인
   └── 팀 카드의 '연락하기' 클릭 → 외부 연락처(카카오오픈채팅 등)로 팀 합류 요청

6. 나의 공간 (/me) → 팀 만들기
   └── 팀 찾기 페이지 '팀 만들기' 버튼 → 폼 작성 → 저장
   └── 나의 공간 '내 팀' 섹션, '참여중인 해커톤' 섹션에 즉시 반영

7. 결과 제출
   └── 해커톤 상세 '제출' 탭 → 기획서/웹링크/PDF 입력 → 제출
   └── 나의 공간 '제출 이력' 섹션에 기록

8. 순위 확인
   └── 랭킹 페이지 → 해커톤 선택 → 전체 리더보드 확인
   └── '나의 랭킹' 탭 → 내 팀 순위만 강조
   └── 나의 공간 통계 카드 '최고 순위' 갱신
```

### 예외 및 오류 처리

| 상황 | 화면 처리 |
|------|-----------|
| 존재하지 않는 해커톤 slug 접근 | `notFound` 상태 활성화 → "해커톤을 찾을 수 없습니다" 안내 메시지 + 목록으로 돌아가기 링크 표시 |
| 프로필 미설정 상태에서 추천 접근 | 추천 캐러셀 자체가 비표시 (role·skills 미설정 시 점수 산출 불가) |
| 팀 만들기 필수 항목 미입력 | Toast 오류 알림("팀 이름과 해커톤을 입력해주세요.") 표시, 저장 차단 |
| 링크 복사 Clipboard API 미지원 | `execCommand` 폴백 자동 실행, 실패 시 Toast 오류 메시지 표시 |
| 필터 결과 없음 | "해당하는 해커톤/팀이 없습니다" 빈 상태 화면 + '필터 초기화' 버튼 표시 |
| 리더보드 데이터 없는 해커톤 | 순위표 영역에 "아직 제출된 결과가 없습니다" 안내 |
| localStorage 읽기/쓰기 실패 | `try-catch`로 조용히 처리, `null` 반환 후 기본값으로 대체 |

---

## 7. 개발 및 개선 계획

### 완료된 기능 (현재 버전)

| 우선순위 | 기능 | 상태 |
|----------|------|------|
| P0 | 해커톤 목록·상세·필터·검색·북마크 | ✅ 완료 |
| P0 | 팀 목록·만들기·수정·삭제·필터 | ✅ 완료 |
| P0 | 리더보드·제출 이력 | ✅ 완료 |
| P0 | 나의 공간 (프로필·통계·참여 해커톤·북마크·내 팀·제출 이력) | ✅ 완료 |
| P1 | AI 맞춤 해커톤/팀 추천 캐러셀 (룰 기반, 70% 필터) | ✅ 완료 |
| P1 | 해커톤 진행률 바, 팀 역할 매칭 배지, 링크 공유 버튼 | ✅ 완료 |
| P1 | 다크 모드, 전역 검색 모달, Toast 알림 | ✅ 완료 |
| P1 | 마감임박순 정렬 개선, 접근성(aria) 적용 | ✅ 완료 |

### 추가 개선 아이디어 (다음 버전)

| 우선순위 | 기능 | 설명 |
|----------|------|------|
| P0 | **실시간 알림** | 관심 대회 마감 D-3 알림, 팀원 모집 신규 팀 등록 알림 |
| P0 | **팀원 지원/수락 흐름** | 현재 '연락하기' 외부 링크 방식 → 플랫폼 내 지원 버튼 + 팀장 수락/거절 기능 |
| P1 | **팀 채팅/게시판** | 팀 내부 소통 공간, 공지사항·파일 공유 |
| P1 | **대회 캘린더 뷰** | 월간 캘린더 형태로 대회 일정 한눈에 확인 |
| P1 | **스킬 기반 팀원 매칭 점수 고도화** | 현재 룰 기반 → 과거 참여 이력·수상 경력 반영한 가중치 개선 |
| P2 | **백엔드 연동** | localStorage → 실제 DB(PostgreSQL 등) 마이그레이션, 멀티 유저 지원 |
| P2 | **소셜 로그인** | Google/GitHub OAuth 연동으로 프로필 공유 및 팀원 검색 가능 |
| P2 | **대회 결과 분석 대시보드** | 팀별 제출 이력, 점수 추이 그래프, 스킬별 수상 통계 시각화 |
| P2 | **모바일 앱** | React Native 또는 PWA로 모바일 알림·오프라인 지원 |

### 기술 부채 및 리팩터링 계획

| 항목 | 현재 상태 | 개선 방향 |
|------|-----------|-----------|
| 검색 debounce | 키 입력마다 즉시 필터링 | `useCallback` + 300ms debounce 적용 |
| 파생 상태 메모이제이션 | 매 렌더링마다 재계산 | `useMemo`로 `allTags`, `filtered` 최적화 |
| 상태 관리 복잡도 | 페이지별 다수 `useState` | Zustand 또는 Context 도입 검토 |
| 테스트 커버리지 | 0% | 추천 로직·정렬 함수에 단위 테스트 추가 |

